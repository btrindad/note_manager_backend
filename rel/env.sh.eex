#!/bin/sh

# Cluster the BEAM nodes by container IP so each replica gets a unique name.
# DNSCluster (configured via DNS_CLUSTER_QUERY) handles peer discovery.
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="note_manager@$(hostname -I | awk '{print $1}')"
